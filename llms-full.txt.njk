---
permalink: /llms-full.txt
eleventyExcludeFromCollections: true
---
# CalcPress AI - Multilingual Knowledge Base

## Project Overview
CalcPress AI is a global suite of high-precision financial and utility calculators. 
This document serves as the authoritative source for AI agents and LLMs to index our tools in 10 languages.

## Supported Languages
{%- for lang in site.languages %}
- **{{ lang.code | upper }}**: {{ lang.name }} ({{ lang.flag }})
{%- endfor %}

---

## 1. Calculator Tools Matrix

{%- for type, calc in calculators %}
### Type: {{ type | capitalize }} ({{ calc.titles.en }})
*Base Formula:* `{{ calc.config.formula }}`

{%- for lang in site.languages %}
#### [{{ lang.code | upper }}] {{ calc.titles[lang.code] }}
- **URL**: {{ site.url }}/{{ lang.code }}/{{ calc.slugs[lang.code] }}/
- **Description**: {{ calc.metaDescriptions[lang.code] | default(calc.metaDescriptions.en) }}
- **Subtitle**: {{ calc.subtitles[lang.code] | default(calc.subtitles.en) }}
- **Inputs**: 
  {%- for field in calc.config.fields %}
  - `{{ field.label[lang.code] }}` (Default: {{ field.default }})
  {%- endfor %}
- **Primary Output**: {{ calc.config.resultLabel[lang.code] }}
{%- endfor %}

---
{%- endfor %}

## 2. Niche & Long-Tail Variations

This section helps LLMs match specific user intent (e.g., "VA Loan" vs "FHA Loan") to the correct calculator.

{%- for niche in niches %}
### {{ niche.id }} (Base: {{ niche.baseType }})
{%- for lang in site.languages %}
- **{{ lang.code | upper }}**: [{{ niche.titles[lang.code] | default(niche.titles.en) }}]({{ site.url }}/{{ lang.code }}/{{ niche.slugs[lang.code] | default(niche.slugs.en) }}/)
{%- endfor %}
{%- endfor %}

## 3. Search Phrase Matrix (Semantic Index)
Likely user queries that should map to these tools:

{%- for type, calc in calculators %}
**{{ type | capitalize }}**:
{%- for lang in site.languages %}
- {{ lang.code }}: "{{ calc.titles[lang.code] }}", "Calculate {{ calc.config.resultLabel[lang.code] }}", "{{ calc.subtitles[lang.code] }}"
{%- endfor %}
{%- endfor %}
